"""
Shared display padding for terminal UI. Used by live blocks and replay so
margins stay consistent.
"""

# (top, right, bottom, left). 2 chars left/right for horizontal margin.
PADDING_MESSAGE = (1, 2, 0, 2)
PADDING_PANEL = (0, 2, 0, 2)
